<UserControl x:Class="Fias.Update.FiasConnectionUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="26" />
            <RowDefinition Height="*" />
            <RowDefinition Height="26" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Параметры подключения к БД" VerticalAlignment="Center" FontSize="16" Padding="5,0,0,0" FontWeight="Bold" />
        <DockPanel Grid.Row="2">
            <TextBlock Text="Строка подключения к БД" VerticalAlignment="Center" Width="155" Padding="5,0"/>
            <TextBox Text="{Binding Path=ConnectionString}" />
        </DockPanel>
    </Grid>
</UserControl>
